/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    '@tailwindcss/postcss': {}, // Age sudhu 'tailwindcss' chilo
    autoprefixer: {},
  },
};

export default config;